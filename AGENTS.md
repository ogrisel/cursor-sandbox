## Cursor Cloud specific instructions

This repository (`ogrisel/cursor-sandbox`) is currently an empty sandbox with only an initial commit containing no tracked files.

- **No build system, dependencies, or services** are configured yet.
- **No lint, test, or run commands** are available until code is added.
- Once the repository gains content, update this section with relevant startup/run caveats.
